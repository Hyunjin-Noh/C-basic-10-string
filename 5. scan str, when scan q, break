#include <stdio.h>
#include <string.h>

int main()
{
    char str[101];
    char str2[101]={0};//초기화
    int len;
    
    while(1){
        scanf("%s", str);
        if (str[0]=='q'&&str[1]=='\0') break;
        strcat(str2, str);
    }
    printf("%s", str2);
    return 0;
}
